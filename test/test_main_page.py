import pytest
from data.login import *


# Проверка имени сотрудника
#@pytest.mark.parametrize("testdata1", valid_data, ids=[repr(valid_data)])
#def test_valid_name(app, testdata1):
#    app.login(testdata1)
#    assert app.find_name() == app.get_full_name(testdata1)

# Проверка модуля сотрудника
#@pytest.mark.parametrize("testdata1", valid_data, ids=[repr(valid_data)])
#def test_valid_module(app, testdata1):
#    app.login(testdata1)
#    assert app.find_module() == app.get_module(testdata1)
